able to add resources and use the resource on web 
able to start new project with different folder name
add db support 
the folder should be written to the outside cli and not push on a same repo i think
low priority: 
auto-rerun everytime the code changes, can manually run now