able to add resources and use the resource on web, may integrate with some marketplace
add db support
add document and actually use while generating the code

low priority: 
embed view in vscode instead of need to open a new terminal to run the server

